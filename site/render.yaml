services:
  - type: web
    name: erpnext-migration-dossier
    runtime: node
    plan: free
    buildCommand: cd site && npm install
    startCommand: cd site && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
